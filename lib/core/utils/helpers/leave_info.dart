class LeaveInfo {
  final String userId;
  final bool isSingleDay;
  final bool isStart;
  final bool isEnd;

  LeaveInfo({
    required this.userId,
    required this.isSingleDay,
    required this.isStart,
    required this.isEnd,
  });
}