enum LeaveActionType { remind, escalate, cancel }

/// Enum for supported HTTP methods
enum HttpMethod { get, post, put, delete }

/// Enum for body type
enum BodyType { json, formUrlEncoded, multipart }

enum ToastType { success, error, info }

enum MyButtonType { elevated, outlined, text }

enum UserRole { employee, manager, hr }

enum LeaveStatus { pending, approved, rejected, escalated }

enum LeaveFormType { leave, extra, workFromHome }
